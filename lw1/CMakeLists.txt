cmake_minimum_required(VERSION 3.15)

set(CURRENT_LAB_NAME "lw1")
project(${CURRENT_LAB_NAME})

# //FIXME Временно отключил уток
# add_subdirectory(ducks)
# add_subdirectory(functionalDucks)
add_subdirectory(shapes)
